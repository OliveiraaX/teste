---
- name: teste com JIRA
  hosts: all
  vars_files:
    - vars.yml
  tasks:
    - name: mostrar variáveis
      shell: "{{ shell1 }}"
      register: shell11

    - name: Exibir resultado do comando
      debug:
        msg: "{{ shell11 }}"
